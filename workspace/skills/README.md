# Skills — Discovery Pointer

Skills (slash commands) live in `.claude/commands/` and are invocable as `/command-name` in
Claude Code or via the Skill tool. All are plain Markdown — any agent can read them directly.

**Canonical inventory + status:** `.claude/GOVERNANCE.md` → Part 2 (AI Infrastructure Inventory).
Do not duplicate the list here.
