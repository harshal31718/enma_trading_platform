# Baked into the client bundle by `npm run build` (Vite production mode).
# Mode-specific files override the git-ignored client/.env used by `npm run dev`.
# Replace yourdomain.com with the real production domain before the first deploy.
VITE_API_URL=https://yourdomain.com
VITE_SOCKET_URL=https://yourdomain.com
